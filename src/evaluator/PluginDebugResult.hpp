#pragma once


/**
 * @brief Encapsulates the debug informations of a plugin's evaluation process,
 * if said plugin support it.
 */
class PluginDebugResult
{
public:
	PluginDebugResult() = default;

	virtual ~PluginDebugResult() = default;
};
